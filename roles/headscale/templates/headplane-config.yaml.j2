server:
  host: "0.0.0.0"
  port: 3000
  base_url: "http://localhost:3000"
  cookie_secret: "{{ hc_apikey.stdout | trim | hash('md5') }}"
  cookie_secure: false

headscale:
  url: "http://headscale:8080"
  config_path: "/etc/headscale/config.yaml"
  api_key: "{{ hc_apikey.stdout | trim }}"
